@model HomeViewModel

<div class="text-center mb-5">
    <h1 class="display-5 fw-semibold">Welcome</h1>
    <p class="text-muted lead">Browse hardware and peripherals</p>
</div>

<section class="mb-5">
    <h2 class="h4 text-center mb-4">Most bought products</h2>
    <partial name="_HomePartialView" model="@Model.MostBoughtProducts"/>
</section>

<section class="mb-4">
    <h2 class="h4 text-center mb-4">Newest products</h2>
    <partial name="_HomePartialView" model="@Model.NewestProducts"/>
</section>
