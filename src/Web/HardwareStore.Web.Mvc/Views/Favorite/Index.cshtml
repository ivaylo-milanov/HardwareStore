@model ICollection<FavoriteExportModel>

<div class="mb-4 text-center text-md-start">
    <h2 class="h3 fw-semibold">Favorite products</h2>
    <p class="text-muted mb-0">Items you saved for later</p>
</div>
<partial name="_FavoritePartialView" model="@Model" />
