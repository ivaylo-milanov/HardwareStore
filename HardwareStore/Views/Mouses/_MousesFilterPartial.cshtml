@model List<ProductNameValueModel>

<ul>
    @foreach (var kvp in Model)
    {
        <li>
            <label>
                <input type="checkbox" name="@ViewData["Name"]" value="@kvp.Value"/> @kvp.Name
            </label>
        </li>
    }
</ul>
